"use strict";
//T_shirts:write a function called  make_shirt(function so that accept a size and the text    
//of a message that should be printed on the shirt.The function should print a sentence
//the size of the shirt and the message printed on it.
//call the function .
function make_shirt(size, text) {
    console.log(`creating a ${size} shirt with message:${text}`);
}
make_shirt(`small`, `hello world`);
